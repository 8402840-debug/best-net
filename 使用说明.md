# 卡片保险柜 - 用 GitHub Actions 云端打包成 exe

这个文件夹里的代码不需要你自己电脑上装 Python 或跑任何打包命令。
GitHub 会在它自己的云端 Windows 电脑上帮你打包，打包完成后你直接下载 exe 文件即可。

## 文件夹内容

```
card-vault-exe/
├── app.py                          启动窗口的Python代码（内容不含你的任何卡片数据）
├── card-vault.html                 卡片管理工具本体（网页版一样的界面）
├── requirements.txt                依赖列表
└── .github/workflows/build.yml     自动打包脚本，交给GitHub云端执行
```

## 操作步骤（全程在浏览器网页里完成，不用装任何软件）

### 第一步：注册/登录 GitHub
打开 https://github.com ，没有账号就免费注册一个。

### 第二步：新建一个仓库
1. 右上角点击 "+" → "New repository"
2. Repository name 随便起，比如 `card-vault-exe`
3. 建议选择 **Private**（私有），这样外人看不到你的仓库
4. 点击 "Create repository"

### 第三步：上传这个文件夹里的所有文件
1. 进入刚创建的仓库页面，点击 "uploading an existing file"（或 "Add file" → "Upload files"）
2. 把这个文件夹（包括里面的 `.github` 子文件夹）整个拖进浏览器上传区域
   - GitHub 网页上传支持保留文件夹结构，直接把整个 `card-vault-exe` 文件夹拖进去即可
3. 拉到底部，点击绿色的 "Commit changes"

### 第四步：查看自动打包进度
1. 点击仓库顶部的 "Actions" 标签
2. 你会看到一个正在运行（或刚完成）的工作流，叫 "Build EXE"
3. 点进去，等它跑完（通常 2-5 分钟），状态会变成绿色的勾 ✓

### 第五步：下载 exe
1. 打包完成后，页面下方会有一个 "Artifacts" 区域
2. 点击 "CardVault-exe" 即可下载一个 zip 压缩包
3. 解压后就是 `CardVault.exe`，双击即可运行

## 之后想改内容/重新打包怎么办？
以后如果想修改卡片工具本身，把新版 `card-vault.html` 替换掉仓库里的旧文件（网页上直接编辑或重新上传覆盖），
每次提交（Commit）都会自动触发重新打包，去 Actions 标签下载新的 exe 即可。

## 安全说明
- 这些文件本身不包含你的任何卡片数据，只是打包工具的"外壳"代码，可以放心上传
- exe 运行后，数据仍然只保存在你电脑本地（浏览器缓存所在的用户目录），不会上传到 GitHub 或任何网络
